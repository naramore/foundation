defmodule Foundation do
  @moduledoc """
  Foundation contains a set of data-flow composition abstractions.

    - `Rails` -> Railway-Oriented Programming
    - `Arrow` -> Arrows (i.e. spliting computation)
    - `Fix` -> Saga Pattern (i.e. effect compensation)
    - `Pend` -> Pause Monad (i.e. interuptible computation)
  """
end
