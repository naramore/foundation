defmodule Monty.Domain.MotorSystem do
  # use GenServer

  # @callback handle_action(action, from, state) :: {}
  # @callback handle_percept(percept, from, state) :: {}

  # @spec start_link(module(), Keyword.t()) :: GenServer.on_start()
  # @spec submit_goal(GenServer.server(), Domain.goal()) :: :ok
  # @spec terminate(GenServer.server()) :: :ok
end
